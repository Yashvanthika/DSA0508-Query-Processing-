import pandas as pd
import numpy as np

df = pd.DataFrame({
    "A": [1, np.nan, np.nan, 4],
    "B": [np.nan, np.nan, 3, 4],
    "C": [1, 2, np.nan, np.nan]
})

print(df)

rows = df[df.isna().sum(axis=1) >= 2]

print("\nRows with at least 2 NaN values:\n")
print(rows)
