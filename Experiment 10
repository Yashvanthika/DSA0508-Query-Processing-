import pandas as pd
import numpy as np

df = pd.DataFrame(np.random.randn(10,4),
                  columns=list("ABCD"))

def color_values(val):
    if val < 0:
        return "color: red"
    else:
        return "color: black"

styled = df.style.applymap(color_values)
styled
